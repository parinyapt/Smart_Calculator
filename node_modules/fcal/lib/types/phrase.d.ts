import { SymbolTable } from '../evaluator/symboltable';
import { TT } from '../parser/lex/token';
declare class Phrases {
    phrases: Map<string, TT>;
    private symbolTable;
    constructor(symbolTable: SymbolTable);
    push(key: TT, phrases: string[]): void;
    get(key: string): TT | undefined;
}
export { Phrases };
